{
  "name": "prompt-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Curated Prompt Card library — VitePress static site build.",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
